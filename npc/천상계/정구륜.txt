승급술사	{
	image 0, 50, 0;
	set @sd, mysd();
	set @id, myid();
	set @myjob, get_job(@sd);
	set @myjoblv, get_joblevel(@sd);

	switch(@myjoblv)
	{
	case 0:
		set @jobname$, "주술사";
		break;
	case 1:
		set @jobname$, "술사";
		break;
	case 2:
		set @jobname$, "현사";
		break;
	case 3:
		set @jobname$, "현인";
		break;
	case 4:
		set @jobname$, "현자";
		break;
	case 5:
		set @jobname$, "마신";
		break;
	case 6:
		set @jobname$, "마제";
		break;
	case 7:
		set @jobname$, "마성";
		break;
	}
RESET:
	set @pos, list2(@ret, 0, 0, "안녕하세요. 어떻게 오셨나요?", @jobname$ + "로서 승급을 원합니다", "기술을 배울래요", "3차승급 옷을 원합니다", "기술각성");
	if(@pos == 0) { end; }
	else if(@ret == 1)
	{
		if(@myjob == 3 && @myjoblv < 3)
		{
			setarray @hp[0], 30000, 150000, 300000, 650000, 1000000;
			setarray @mp[0], 20000, 100000, 500000, 900000, 1200000;

			if(get_level(@sd) >= 99 && get_purehp(@sd) >= @hp[@myjoblv] && get_puremp(@sd) >= @mp[@myjoblv])
			{
				if(@myjoblv == 0) { setarray @needitem$[0], "팔괘"; }
				else if(@myjoblv == 1) { setarray @needitem$[0], "수룡의비늘","화룡의비늘"; }
				else if(@myjoblv == 2) { setarray @needitem$[0], "천성현의증표"; }
				else if(@myjoblv == 3) { setarray @needitem$[0],"반고의심장"; }

				for(set @i, 0; @i < getarraysize(@needitem$); set @i, @i+1)
				{
					if(countitem(name2itemid(@needitem$[@i])) == 0)
					{
						set @pos, dialog(0,0,0, @needitem$[@i] + " (이)가 없습니다.");
						deletearray @needitem$[0], getarraysize(@needitem$);
						deletearray @hp[0], getarraysize(@hp);
						deletearray @mp[0], getarraysize(@mp);
						end;
					}
				}
				if(@i == getarraysize(@needitem$))
				{
					for(set @i, 0; @i < getarraysize(@needitem$); set @i, @i+1)
					{
						delitem @sd,name2itemid(@needitem$[@i]),1;
					}

					set_joblevel @sd, @myjoblv+1;

					spellaether @sd, name2spellid("승급보상"), 1;

					broadcast 4, "(( [" + get_name(@sd) + "]님이 " + get_jobname(@sd) + "으로 승급하였습니다. 축하합니다! ))";
				}
			}
			else
			{
				set @pos, dialog(0,0,0, "좀더 수련을 쌓고오세요.");
			}
			deletearray @needitem$[0], getarraysize(@needitem$);
			deletearray @hp[0], getarraysize(@hp);
			deletearray @mp[0], getarraysize(@mp);
			end;
		}
		else
		{
			set @pos, dialog(0, 0, 1, "당신은 더 이상 제가 수련을 도와드리지 않아도 될 만큼 성장하셨군요.");
			end;
		}
	}
	else if(@ret == 2)
	{
		if(@myjob == 3)
		{
			set @ret, list1("안녕하세요. 어떻게 오셨나요?", "1차 승급기술", "2차 승급기술", "3차 승급기술", "4차 승급기술", "5차 승급기술", "6차 승급기술", "7차 승급기술");
			if(@ret == 1 && @myjoblv >= 1)
			{
				setarray @magic$[0], "태양의기원";
				set @ret, list1("안녕하세요. 어떤 기술을 배울래요?", @magic$[0]);
				if(@ret == 1)
				{
					set @pos, dialog(0, 0, 1, @magic$[@ret-1] + "을 배우기 위해선 5000만의 경험치가 필요합니다.");
					if(@pos == 0) { deletearray @magic$[0], getarraysize(@magic$); end; }

					if(get_exp(@sd) < 0) { set @exp1, 2147483647; set @exp2, (get_exp(@sd)+@exp1); }
					else { set @exp1, get_exp(@sd); set @exp2, 0; }
					
					if((@exp1-50000000) < 0) { set @pos, dialog(0, 0, 0, "경험치가 모자랍니다."); deletearray @magic$[0], getarraysize(@magic$); end; }
					if(checkspell(name2spellid(@magic$[@ret-1])) == 52 )
					{
						set_exp @sd, ((@exp1-50000000)+@exp2);
						addspell name2spellid(@magic$[@ret-1]);
						set @pos, dialog(0, 0, 0, @magic$[@ret-1] + "마법을 드렸습니다.");
						updatestatus @sd, 2;
					}
					else { set @pos, dialog(0, 0, 0, "이미 배운 마법입니다."); deletearray @magic$[0], getarraysize(@magic$); end; }
				}
				deletearray @magic$[0], getarraysize(@magic$);
			}
			if(@ret == 2 && @myjoblv >= 2)
			{
				setarray @magic$[0], "삼매진화", "호체주술";
				set @ret, list1("안녕하세요. 어떤 기술을 배울래요?", @magic$[0], @magic$[1]);
				if(@ret == 0) { end; }
				set @pos, dialog(0, 0, 1, @magic$[@ret-1] + "을 배우기 위해선 5000만의 경험치가 필요합니다.");
				if(@pos == 0) { deletearray @magic$[0], getarraysize(@magic$); end; }

				if(get_exp(@sd) < 0) { set @exp1, 2147483647; set @exp2, (get_exp(@sd)+@exp1); }
				else { set @exp1, get_exp(@sd); set @exp2, 0; }
					
				if((@exp1-50000000) < 0) { set @pos, dialog(0, 0, 0, "경험치가 모자랍니다."); deletearray @magic$[0], getarraysize(@magic$); end; }
				if(checkspell(name2spellid(@magic$[@ret-1])) == 52 )
				{
					set_exp @sd, ((@exp1-50000000)+@exp2);
					addspell name2spellid(@magic$[@ret-1]);
					set @pos, dialog(0, 0, 0, @magic$[@ret-1] + "마법을 드렸습니다.");
					updatestatus @sd, 2;
				}
				else { set @pos, dialog(0, 0, 0, "이미 배운 마법입니다."); deletearray @magic$[0], getarraysize(@magic$); end; }
				deletearray @magic$[0], getarraysize(@magic$);
			}
			if(@ret == 3 && @myjoblv >= 3)
			{
				setarray @magic$[0], "마기지체", "지폭지술", "노도성황", "환기", "만공", "염화", "수폭지술";
				set @ret, list1("안녕하세요. 어떤 기술을 배울래요?", @magic$[0], @magic$[1], @magic$[2], @magic$[3], @magic$[4], @magic$[5], @magic$[6]);
				if(@ret == 0) { end; }
				set @pos, dialog(0, 0, 1, @magic$[@ret-1] + "을 배우기 위해선 5000만의 경험치가 필요합니다.");
				if(@pos == 0) { deletearray @magic$[0], getarraysize(@magic$); end; }

				if(get_exp(@sd) < 0) { set @exp1, 2147483647; set @exp2, (get_exp(@sd)+@exp1); }
				else { set @exp1, get_exp(@sd); set @exp2, 0; }
					
				if((@exp1-50000000) < 0) { set @pos, dialog(0, 0, 0, "경험치가 모자랍니다."); deletearray @magic$[0], getarraysize(@magic$); end; }
				if(checkspell(name2spellid(@magic$[@ret-1])) == 52 )
				{
					set_exp @sd, ((@exp1-50000000)+@exp2);
					addspell name2spellid(@magic$[@ret-1]);
					set @pos, dialog(0, 0, 0, @magic$[@ret-1] + "마법을 드렸습니다.");
					updatestatus @sd, 2;
				}
				else { set @pos, dialog(0, 0, 0, "이미 배운 마법입니다."); deletearray @magic$[0], getarraysize(@magic$); end; }
				deletearray @magic$[0], getarraysize(@magic$);
			}
			if(@ret == 4 && @myjoblv >= 4)
			{
				setarray @magic$[0], "폭류유성", "현자의기원", "만파지독", "탈명사식'지";
				set @ret, list1("안녕하세요. 어떤 기술을 배울래요?", @magic$[0], @magic$[1], @magic$[2], @magic$[3]);
				if(@ret == 0) { end; }
				set @pos, dialog(0, 0, 1, @magic$[@ret-1] + "을 배우기 위해선 5000만의 경험치가 필요합니다.");
				if(@pos == 0) { deletearray @magic$[0], getarraysize(@magic$); end; }

				if(get_exp(@sd) < 0) { set @exp1, 2147483647; set @exp2, (get_exp(@sd)+@exp1); }
				else { set @exp1, get_exp(@sd); set @exp2, 0; }
					
				if((@exp1-50000000) < 0) { set @pos, dialog(0, 0, 0, "경험치가 모자랍니다."); deletearray @magic$[0], getarraysize(@magic$); end; }
				if(checkspell(name2spellid(@magic$[@ret-1])) == 52 )
				{
					set_exp @sd, ((@exp1-50000000)+@exp2);
					addspell name2spellid(@magic$[@ret-1]);
					set @pos, dialog(0, 0, 0, @magic$[@ret-1] + "마법을 드렸습니다.");
					updatestatus @sd, 2;
				}
				else { set @pos, dialog(0, 0, 0, "이미 배운 마법입니다."); deletearray @magic$[0], getarraysize(@magic$); end; }
				deletearray @magic$[0], getarraysize(@magic$);
			}
			if(@ret == 5 && @myjoblv >= 5)
			{
				setarray @magic$[0], "십자소상파환겁", "마신의기원", "사슬벼락";
				set @ret, list1("안녕하세요. 어떤 기술을 배울래요?", @magic$[0], @magic$[1], @magic$[2]);
				if(@ret == 0) { end; }
				set @pos, dialog(0, 0, 1, @magic$[@ret-1] + "을 배우기 위해선 5000만의 경험치가 필요합니다.");
				if(@pos == 0) { deletearray @magic$[0], getarraysize(@magic$); end; }

				if(get_exp(@sd) < 0) { set @exp1, 2147483647; set @exp2, (get_exp(@sd)+@exp1); }
				else { set @exp1, get_exp(@sd); set @exp2, 0; }
					
				if((@exp1-50000000) < 0) { set @pos, dialog(0, 0, 0, "경험치가 모자랍니다."); deletearray @magic$[0], getarraysize(@magic$); end; }
				if(checkspell(name2spellid(@magic$[@ret-1])) == 52 )
				{
					set_exp @sd, ((@exp1-50000000)+@exp2);
					addspell name2spellid(@magic$[@ret-1]);
					set @pos, dialog(0, 0, 0, @magic$[@ret-1] + "마법을 드렸습니다.");
					updatestatus @sd, 2;
				}
				else { set @pos, dialog(0, 0, 0, "이미 배운 마법입니다."); deletearray @magic$[0], getarraysize(@magic$); end; }
				deletearray @magic$[0], getarraysize(@magic$);
			}
			if(@ret == 6 && @myjoblv >= 6)
			{
				setarray @magic$[0], "헬아이스", "천지창조";
				set @ret, list1("안녕하세요. 어떤 기술을 배울래요?", @magic$[0], @magic$[1]);
				if(@ret == 0) { end; }
				set @pos, dialog(0, 0, 1, @magic$[@ret-1] + "을 배우기 위해선 5000만의 경험치가 필요합니다.");
				if(@pos == 0) { deletearray @magic$[0], getarraysize(@magic$); end; }

				if(get_exp(@sd) < 0) { set @exp1, 2147483647; set @exp2, (get_exp(@sd)+@exp1); }
				else { set @exp1, get_exp(@sd); set @exp2, 0; }
					
				if((@exp1-50000000) < 0) { set @pos, dialog(0, 0, 0, "경험치가 모자랍니다."); deletearray @magic$[0], getarraysize(@magic$); end; }
				if(checkspell(name2spellid(@magic$[@ret-1])) == 52 )
				{
					set_exp @sd, ((@exp1-50000000)+@exp2);
					addspell name2spellid(@magic$[@ret-1]);
					set @pos, dialog(0, 0, 0, @magic$[@ret-1] + "마법을 드렸습니다.");
					updatestatus @sd, 2;
				}
				else { set @pos, dialog(0, 0, 0, "이미 배운 마법입니다."); deletearray @magic$[0], getarraysize(@magic$); end; }
				deletearray @magic$[0], getarraysize(@magic$);
			}
			if(@ret == 7 && @myjoblv >= 7)
			{
				setarray @magic$[0], "마성강림", "마성의기원", "폭염왕의강림", "무간지옥", "혹한", "세계후";
				set @ret, list1("안녕하세요. 어떤 기술을 배울래요?", @magic$[0], @magic$[1], @magic$[2], @magic$[3], @magic$[4], @magic$[5]);
				if(@ret == 0) { end; }
				set @pos, dialog(0, 0, 1, @magic$[@ret-1] + "을 배우기 위해선 5000만의 경험치가 필요합니다.");
				if(@pos == 0) { deletearray @magic$[0], getarraysize(@magic$); end; }

				if(get_exp(@sd) < 0) { set @exp1, 2147483647; set @exp2, (get_exp(@sd)+@exp1); }
				else { set @exp1, get_exp(@sd); set @exp2, 0; }
					
				if((@exp1-50000000) < 0) { set @pos, dialog(0, 0, 0, "경험치가 모자랍니다."); deletearray @magic$[0], getarraysize(@magic$); end; }
				if(checkspell(name2spellid(@magic$[@ret-1])) == 52 )
				{
					set_exp @sd, ((@exp1-50000000)+@exp2);
					addspell name2spellid(@magic$[@ret-1]);
					set @pos, dialog(0, 0, 0, @magic$[@ret-1] + "마법을 드렸습니다.");
					updatestatus @sd, 2;
				}
				else { set @pos, dialog(0, 0, 0, "이미 배운 마법입니다."); deletearray @magic$[0], getarraysize(@magic$); end; }
				deletearray @magic$[0], getarraysize(@magic$);
			}
			else
			{
				goto RESET;
			}
		}
	}
	else if(@ret == 3)
	{
		if(@myjoblv < 3)
		{
			set @pos, dialog(0, 0, 1, "3차 승급을 한 뒤에 다시 오세요.");
			if(@pos == 0) { end; }
		}
	}
	else if(@ret == 4)
	{
		set @menu, list1("연마할 마법을 골라주세요.", "염화", "마력방패", "지옥인화");
		if(@menu == 1)
		if(get_joblevel(@sd) == 5)
		{
		if(get_purehp(@sd) < 1100000) || (get_puremp(@sd) < 1300000)
		{
			set @pos, dialog(0,0,0, "체력, 마력조건이 충족되지 않았습니다.");
			end;
		}
		else
		
			set @pos, list2(@ret, 0, 0, "염화'진을 배우시겠습니까","네","아니요");
			if(@pos == 0) end;
			if(@ret == 1)
			{
				if(checkspell(name2spellid("염화'진")) != 52 )
				{
					set @pos, dialog(0, 0, 1, "이미 염화'진을 배웠구나");
					end;
				}
				if(checkspell(name2spellid("염화")) == 52 )
				{
					set @pos, dialog(0, 0, 1, "염화을 배우고 오너라");
					end;
				}
				for(set @i, 0; @i < 52; set @i, @i+1)
				{
					if(checkspell(name2spellid("염화")) == @i)
					{
						delspell @i;
						break;
					}
				}
				addspell name2spellid("염화'진");
				set @pos, dialog(0, 0, 0, "염화'진을 배웠습니다.");
				end;
			}
		}
		if(@menu == 2)
		if(get_joblevel(@sd) == 5)
		{
		if(get_purehp(@sd) < 1100000) || (get_puremp(@sd) < 1300000)
		{
			set @pos, dialog(0,0,0, "체력, 마력조건이 충족되지 않았습니다.");
			end;
		}
		else
		
			set @pos, list2(@ret, 0, 0, "마력방패를 배우시겠습니까","네","아니요");
			if(@pos == 0) end;
			if(@ret == 1)
			{
				if(checkspell(name2spellid("마력방패")) != 52 )
				{
					set @pos, dialog(0, 0, 1, "이미 마력방패를 배웠구나");
					end;
				}
				if(checkspell(name2spellid("호체주술")) == 52 )
				{
					set @pos, dialog(0, 0, 1, "호체주술을 배우고 오너라");
					end;
				}
				for(set @i, 0; @i < 52; set @i, @i+1)
				{
					if(checkspell(name2spellid("호체주술")) == @i)
					{
						delspell @i;
						break;
					}
				}
				addspell name2spellid("마력방패");
				set @pos, dialog(0, 0, 0, "마력방패를 배웠습니다.");
				end;
			}
		}
		if(@menu == 3)
		if(get_joblevel(@sd) == 5)
		{
		if(get_purehp(@sd) < 1200000) || (get_puremp(@sd) < 1500000)
		{
			set @pos, dialog(0,0,0, "체력, 마력조건이 충족되지 않았습니다.");
			end;
		}
		else
		
			set @pos, list2(@ret, 0, 0, "지옥인화를 배우시겠습니까","네","아니요");
			if(@pos == 0) end;
			if(@ret == 1)
			{
				if(checkspell(name2spellid("지옥인화")) != 52 )
				{
					set @pos, dialog(0, 0, 1, "이미 지옥인화를 배웠구나");
					end;
				}
				if(checkspell(name2spellid("헬파이어")) == 52 )
				{
					set @pos, dialog(0, 0, 1, "헬파이어를 배우고 오너라");
					end;
				}
				for(set @i, 0; @i < 52; set @i, @i+1)
				{
					if(checkspell(name2spellid("헬파이어")) == @i)
					{
						delspell @i;
						break;
					}
				}
				addspell name2spellid("지옥인화");
				set @pos, dialog(0, 0, 0, "지옥인화를 배웠습니다.");
				end;
			}
		}
	}
}