@Èä¾Ç¹ü°ø°İ2	{
	set @id, objid();
	set @rand, rand(1, 10);
	set @rate, rand(1, 10);
	if(rand(1, 10) <= 2)
	{
		callfunc "MONSTER_RANGE_ATTACK", objid(), 10, 6, -1, 107, 104, rand(1800000, 2000000), 1, "ÁöÆøÁö¼ú", 10, "-1";	
	}
}

@Èä¾Ç¹ü°ø°İ1	{
	set @id, myid();
	set @side, get_side(@id);
	set @rand, rand(1, 3);
	if(@rand <= 3)
	{
	for(set @i, 1; @i < 5; set @i, @i+1)
	{
		switch(@side)
		{
		case 0:
			set @x, get_x(@id);
			set @y, get_y(@id)-@i;
			break;
		case 1:
			set @x, get_x(@id)+@i;
			set @y, get_y(@id);
			break;
		case 2:
			set @x, get_x(@id);
			set @y, get_y(@id)+@i;
			break;
		case 3:
			set @x, get_x(@id)-@i;
			set @y, get_y(@id);
			break;
		}

		set @target_id, obj_getxy(@x, @y);
		if((@x >= mapwidth() || @x < 0) || (@y >= mapheight() || @y < 0 )) { break; }
		else if(@i != 4 && canmove(@id, @x, @y)) { break; };
		
		if(@target_id > 0)
		{
			if(@i != 4) { break; }
			else
			{
				if(istype(@target_id) != 3)
				{
					switch(@side)
					{
					case 0:
						set_y @target_id, get_y(@id)-1;
						set_side @target_id, 2; set @side, 2;
						break;
					case 1:
						set_x @target_id, get_x(@id)+1;
						set_side @target_id, 3; set @side, 3;
						break;
					case 2:
						set_y @target_id, get_y(@id)+1;
						set_side @target_id, 0; set @side, 0;
						break;
					case 3:
						set_x @target_id, get_x(@id)-1;
						set_side @target_id, 1; set @side, 1;
						break;
					}
					if(istype(@target_id) == 1)
					{
						set @target_sd, id2sd(@target_id);
//						updatexy @target_sd;
//						updatelookpc @target_sd, 0;
						updateallpc @target_sd;
						say @id, 2, get_mobname(@id) + ": ÃÊÈ¥ºñ¹«~~!~!";
					}
					else
					{
						move @target_id, @side;
					}
					callfunc "cho_MONSTER_RANGE_ATTACK", objid(), 10, 2, -1, 99, 105, rand(1550000, 1700000), 0, "ÃÊÈ¥ºñ¹«~~!~!", 16, "-1";
				}
			}
		}
	}
}
	else if(@rand <= 2)
	{
	for(set @i, 1; @i < 5; set @i, @i+1)
	{
		switch(@side)
		{
		case 0:
			set @x, get_x(@id);
			set @y, get_y(@id)-@i;
			break;
		case 1:
			set @x, get_x(@id)+@i;
			set @y, get_y(@id);
			break;
		case 2:
			set @x, get_x(@id);
			set @y, get_y(@id)+@i;
			break;
		case 3:
			set @x, get_x(@id)-@i;
			set @y, get_y(@id);
			break;
		}
		
		set @target_id, obj_getxy(@x, @y);
		if((@x >= mapwidth() || @x < 0) || (@y >= mapheight() || @y < 0 )) { break; }
		else if(@i != 4 && canmove(@id, @x, @y)) { break; };
		
		if(@target_id > 0)
		{
			if(@i != 4) { break; }
			else
			{
				switch(@side)
				{
				case 0:
					set_y @id, get_y(@target_id)+1;
					break;
				case 1:
					set_x @id, get_x(@target_id)-1;
					break;
				case 2:
					set_y @id, get_y(@target_id)-1;
					break;
				case 3:
					set_x @id, get_x(@target_id)+1;
					break;
				}
				updatelook @id;
				say @id, 2, get_mobname(@id) + ": ¼âÈ¥ºñ¹«~~!~!";
				callfunc "cho_MONSTER_RANGE_ATTACK", objid(), 10, 2, -1, 100, 97, rand(1800000, 2000000), 0, "¼âÈ¥ºñ¹«~~!~!", 16, "-1";
				}
			}
		}
	}
	else if(@rand <= 1)
	{
		set @target_id, obj_getfront();
		if(@target_id <= 0) end;

		switch(@side)
		{
		case 0:
			set @val, canmove(@id, get_x(@id), get_y(@id)-2);
			set_side @id, 2;
			set @move_x, get_x(@id);
			set @move_y, get_y(@id)-2;
			if(@val == 1) // ¾ÈµÉ¶§
			{
			set @val, canmove(@id, get_x(@id)+1, get_y(@id)-1);
			set_side @id, 3;
			set @move_x, get_x(@id)+1;
			set @move_y, get_y(@id)-1;
			if(@val == 1) // ¾ÈµÉ¶§
			{
				set @val, canmove(@id, get_x(@id)-1, get_y(@id)-1);
				set_side @id, 1;
				set @move_x, get_x(@id)-1;
				set @move_y, get_y(@id)-1;
				if(@val == 1)
				{
					set_side @id, 0;
					set @move_x, 0;
					set @move_y, 0;
				}
			}
		}
		break;
	case 1:
		set @val, canmove(@id, get_x(@id)+2, get_y(@id));
		set_side @id, 3;
		set @move_x, get_x(@id)+2;
		set @move_y, get_y(@id);
		if(@val == 1) // ¾ÈµÉ¶§
		{
			set @val, canmove(@id, get_x(@id)+1, get_y(@id)+1);
			set_side @id, 0;
			set @move_x, get_x(@id)+1;
			set @move_y, get_y(@id)+1;
			if(@val == 1) // ¾ÈµÉ¶§
			{
				set @val, canmove(@id, get_x(@id)+1, get_y(@id)-1);
				set_side @id, 2;
				set @move_x, get_x(@id)+1;
				set @move_y, get_y(@id)-1;
				if(@val == 1)
				{
					set_side @id, 1;
					set @move_x, 0;
					set @move_y, 0;
				}
			}
		}
		break;
	case 2:
		set @val, canmove(@id, get_x(@id), get_y(@id)+2);
		set_side @id, 0;
		set @move_x, get_x(@id);
		set @move_y, get_y(@id)+2;
		if(@val == 1) // ¾ÈµÉ¶§
		{
			set @val, canmove(@id, get_x(@id)-1, get_y(@id)+1);
			set_side @id, 1;
			set @move_x, get_x(@id)-1;
			set @move_y, get_y(@id)+1;
			if(@val == 1) // ¾ÈµÉ¶§
			{
				set @val, canmove(@id, get_x(@id)+1, get_y(@id)+1);
				set_side @id, 3;
				set @move_x, get_x(@id)+1;
				set @move_y, get_y(@id)+1;
				if(@val == 1)
				{
					set_side @id, 2;
					set @move_x, 0;
					set @move_y, 0;
				}
			}
		}
		break;
	case 3:
		set @val, canmove(@id, get_x(@id)-2, get_y(@id));
		set_side @id, 1;
		set @move_x, get_x(@id)-2;
		set @move_y, get_y(@id);
		if(@val == 1) // ¾ÈµÉ¶§
		{
			set @val, canmove(@id, get_x(@id)-1, get_y(@id)-1);
			set_side @id, 2;
			set @move_x, get_x(@id)-1;
			set @move_y, get_y(@id)-1;
			if(@val == 1) // ¾ÈµÉ¶§
			{
				set @val, canmove(@id, get_x(@id)-1, get_y(@id)+1);
				set_side @id, 0;
				set @move_x, get_x(@id)-1;
				set @move_y, get_y(@id)+1;
				if(@val == 1)
				{
					set_side @id, 3;
					set @move_x, 0;
					set @move_y, 0;
				}
			}
		}
		break;
	}
	
	if(@move_x != 0 && @move_y != 0)
	{
		if(rand(0, 100) < 25 && rand(0, 100) < 25)
		{
			set_side @id, @side;
		}
		else
		{
			set_xy @id, @move_x, @move_y;
			damage @id, @target_id, 0, 1;
		}
		updatelook @id;

		action @id, 1, 15, 30;
		sound @target_id, 30;
		}
	}
}

@Èä¾Ç¹ü°ø°İ3	{
	set @id, myid();
	set @side, get_side(@id);
	set @rand, rand(1, 3);
	if(@rand <= 3)
	{
	for(set @i, 1; @i < 5; set @i, @i+1)
	{
		switch(@side)
		{
		case 0:
			set @x, get_x(@id);
			set @y, get_y(@id)-@i;
			break;
		case 1:
			set @x, get_x(@id)+@i;
			set @y, get_y(@id);
			break;
		case 2:
			set @x, get_x(@id);
			set @y, get_y(@id)+@i;
			break;
		case 3:
			set @x, get_x(@id)-@i;
			set @y, get_y(@id);
			break;
		}

		set @target_id, obj_getxy(@x, @y);
		if((@x >= mapwidth() || @x < 0) || (@y >= mapheight() || @y < 0 )) { break; }
		else if(@i != 4 && canmove(@id, @x, @y)) { break; };
		
		if(@target_id > 0)
		{
			if(@i != 4) { break; }
			else
			{
				if(istype(@target_id) != 3)
				{
					switch(@side)
					{
					case 0:
						set_y @target_id, get_y(@id)-1;
						set_side @target_id, 2; set @side, 2;
						break;
					case 1:
						set_x @target_id, get_x(@id)+1;
						set_side @target_id, 3; set @side, 3;
						break;
					case 2:
						set_y @target_id, get_y(@id)+1;
						set_side @target_id, 0; set @side, 0;
						break;
					case 3:
						set_x @target_id, get_x(@id)-1;
						set_side @target_id, 1; set @side, 1;
						break;
					}
					if(istype(@target_id) == 1)
					{
						set @target_sd, id2sd(@target_id);
//						updatexy @target_sd;
//						updatelookpc @target_sd, 0;
						updateallpc @target_sd;
						say @id, 2, get_mobname(@id) + ": ÃÊÈ¥ºñ¹«~~!~!";
					}
					else
					{
						move @target_id, @side;
					}
					callfunc "cho_MONSTER_RANGE_ATTACK", objid(), 10, 2, -1, 99, 105, rand(1800000, 2000000), 0, "ÃÊÈ¥ºñ¹«~~!~!", 16, "-1";
				}
			}
		}
	}
}
	else if(@rand <= 2)
	{
	for(set @i, 1; @i < 5; set @i, @i+1)
	{
		switch(@side)
		{
		case 0:
			set @x, get_x(@id);
			set @y, get_y(@id)-@i;
			break;
		case 1:
			set @x, get_x(@id)+@i;
			set @y, get_y(@id);
			break;
		case 2:
			set @x, get_x(@id);
			set @y, get_y(@id)+@i;
			break;
		case 3:
			set @x, get_x(@id)-@i;
			set @y, get_y(@id);
			break;
		}
		
		set @target_id, obj_getxy(@x, @y);
		if((@x >= mapwidth() || @x < 0) || (@y >= mapheight() || @y < 0 )) { break; }
		else if(@i != 4 && canmove(@id, @x, @y)) { break; };
		
		if(@target_id > 0)
		{
			if(@i != 4) { break; }
			else
			{
				switch(@side)
				{
				case 0:
					set_y @id, get_y(@target_id)+1;
					break;
				case 1:
					set_x @id, get_x(@target_id)-1;
					break;
				case 2:
					set_y @id, get_y(@target_id)-1;
					break;
				case 3:
					set_x @id, get_x(@target_id)+1;
					break;
				}
				updatelook @id;
				say @id, 2, get_mobname(@id) + ": ¼âÈ¥ºñ¹«~~!~!";
				callfunc "cho_MONSTER_RANGE_ATTACK", objid(), 10, 2, -1, 100, 97, rand(180000, 2000000), 0, "¼âÈ¥ºñ¹«~~!~!", 16, "-1";
				}
			}
		}
	}
	else if(@rand <= 1)
	{
		set @target_id, obj_getfront();
		if(@target_id <= 0) end;

		switch(@side)
		{
		case 0:
			set @val, canmove(@id, get_x(@id), get_y(@id)-2);
			set_side @id, 2;
			set @move_x, get_x(@id);
			set @move_y, get_y(@id)-2;
			if(@val == 1) // ¾ÈµÉ¶§
			{
			set @val, canmove(@id, get_x(@id)+1, get_y(@id)-1);
			set_side @id, 3;
			set @move_x, get_x(@id)+1;
			set @move_y, get_y(@id)-1;
			if(@val == 1) // ¾ÈµÉ¶§
			{
				set @val, canmove(@id, get_x(@id)-1, get_y(@id)-1);
				set_side @id, 1;
				set @move_x, get_x(@id)-1;
				set @move_y, get_y(@id)-1;
				if(@val == 1)
				{
					set_side @id, 0;
					set @move_x, 0;
					set @move_y, 0;
				}
			}
		}
		break;
	case 1:
		set @val, canmove(@id, get_x(@id)+2, get_y(@id));
		set_side @id, 3;
		set @move_x, get_x(@id)+2;
		set @move_y, get_y(@id);
		if(@val == 1) // ¾ÈµÉ¶§
		{
			set @val, canmove(@id, get_x(@id)+1, get_y(@id)+1);
			set_side @id, 0;
			set @move_x, get_x(@id)+1;
			set @move_y, get_y(@id)+1;
			if(@val == 1) // ¾ÈµÉ¶§
			{
				set @val, canmove(@id, get_x(@id)+1, get_y(@id)-1);
				set_side @id, 2;
				set @move_x, get_x(@id)+1;
				set @move_y, get_y(@id)-1;
				if(@val == 1)
				{
					set_side @id, 1;
					set @move_x, 0;
					set @move_y, 0;
				}
			}
		}
		break;
	case 2:
		set @val, canmove(@id, get_x(@id), get_y(@id)+2);
		set_side @id, 0;
		set @move_x, get_x(@id);
		set @move_y, get_y(@id)+2;
		if(@val == 1) // ¾ÈµÉ¶§
		{
			set @val, canmove(@id, get_x(@id)-1, get_y(@id)+1);
			set_side @id, 1;
			set @move_x, get_x(@id)-1;
			set @move_y, get_y(@id)+1;
			if(@val == 1) // ¾ÈµÉ¶§
			{
				set @val, canmove(@id, get_x(@id)+1, get_y(@id)+1);
				set_side @id, 3;
				set @move_x, get_x(@id)+1;
				set @move_y, get_y(@id)+1;
				if(@val == 1)
				{
					set_side @id, 2;
					set @move_x, 0;
					set @move_y, 0;
				}
			}
		}
		break;
	case 3:
		set @val, canmove(@id, get_x(@id)-2, get_y(@id));
		set_side @id, 1;
		set @move_x, get_x(@id)-2;
		set @move_y, get_y(@id);
		if(@val == 1) // ¾ÈµÉ¶§
		{
			set @val, canmove(@id, get_x(@id)-1, get_y(@id)-1);
			set_side @id, 2;
			set @move_x, get_x(@id)-1;
			set @move_y, get_y(@id)-1;
			if(@val == 1) // ¾ÈµÉ¶§
			{
				set @val, canmove(@id, get_x(@id)-1, get_y(@id)+1);
				set_side @id, 0;
				set @move_x, get_x(@id)-1;
				set @move_y, get_y(@id)+1;
				if(@val == 1)
				{
					set_side @id, 3;
					set @move_x, 0;
					set @move_y, 0;
				}
			}
		}
		break;
	}
	
	if(@move_x != 0 && @move_y != 0)
	{
		if(rand(0, 100) < 25 && rand(0, 100) < 25)
		{
			set_side @id, @side;
		}
		else
		{
			set_xy @id, @move_x, @move_y;
			damage @id, @target_id, 0, 1;
		}
		updatelook @id;

		action @id, 1, 15, 30;
		sound @target_id, 30;
		}
	}
	if(rand(1, 2) == 1)
	{
		callfunc "MONSTER_FRONT_ATTACK", objid(), 10, 3, -1, 18, 9, rand(2400000, 2450000), 2, "µ¿±Í¾îÁø", "-1";
	}
}

@Èä¾Ç¹ü°ø°İ4	{
	set @id, objid();
	set @rand, rand(1, 10);
	set @rate, rand(1, 10);
	if(rand(1, 10) <= 2)
	{
		callfunc "MONSTER_RANGE_ATTACK", objid(), 10, 6, -1, 107, 104, rand(2000000, 2200000), 1, "ÁöÆøÁö¼ú", 10, "-1";	
	}
}
@Èä¾Ç¹ü°ø°İ5	{
	set @id, objid();
	set @rand, rand(1, 5);
	if(@rand <= 3)
	{
		callfunc "MONSTER_FRONT_ATTACK", @id, 10, 8, -1, 90, 69, rand(20000000, 22000000), 0, "¾Ï»ì", "¾Ï»ì";
	}
}
