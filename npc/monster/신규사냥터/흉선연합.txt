@Èä¼±Æò¹Î°ø°İ	{
	set @id, objid();
	set @rand, rand(1, 10);
	if(@rand <= 3)
	{
	set @x1, get_x(@id)-13;
	set @x2, get_x(@id)+13;
	set @y1, get_y(@id)-13;
	set @y2, get_y(@id)+13;
		if (@x1 < 0) { set @x1, 0; }
		if (@x2 > mapwidth()-1) { set @x2, mapwidth()-1; }
		if (@y1 < 0) { set @y1, 0; }
		if (@y2 > mapheight()-1) { set @y2, mapheight()-1; }

		for (set @i, @x1; @i <= @x2; set @i, @i+1)
		{
			for (set @j, @y1; @j<=@y2; set @j, @j+1)
			{
				set @target_id, obj_getxy(@i, @j);
				if(@target_id > 0)
				{
					if(istype(@target_id) == 2)
					{
						if(get_mobhp(@target_id) <= 8000000)
						{
							effect @id, 63;
							effect @target_id, 63; //½Å·ÉÀÌÆåÆ®
							sound @id, 97; //½Å·ÉÈ¿°úÀ½
							set_mobhp @target_id, get_mobhp(@target_id) + 500000;
							say @id, 2, get_mobname(@id) + ": È¸º¹!!!..";
							showgage @target_id; //Ã¼·Â¹Ù Ç¥½Ã
						}
					}
				}
			}
		}
	}
	if(@rand <= 2)
	{
		set @target_id, get_mobtarget(@id);
		set @target_sd, id2sd(@target_id);
		if(@target_sd > 0)
		{
			if(@rate <= 5 && spellaethered(@target_sd, name2spellid("È¥¸¶¼ú")) == 0 && spellaethered(@target_sd, name2spellid("ÀúÁÖ")) == 0)
			{
				effect @target_id, 39;
				sound @target_id, 79;
				set_ac @target_sd, get_ac(@target_sd)+50;
				spellaether @target_sd, name2spellid("È¥¸¶¼ú"), 100;
				message @target_sd, 3, get_mobname(@id) + "ÀÌ(°¡) È¥¸¶¼úÀ» °¡ÇÕ´Ï´Ù";
			}
		}
	}
	if(@rand <= 2)
	{
		set @target_id, get_mobtarget(@id);
		set @target_sd, id2sd(@target_id);
		if(@target_sd > 0)
		{
			if(@rate <= 5 && spellaethered(@target_sd, name2spellid("È¥¸¶¼ú")) == 0 && spellaethered(@target_sd, name2spellid("ÀúÁÖ")) == 0)
			{
				effect @target_id, 13;
				sound @target_id, 69;
				set_ac @target_sd, get_ac(@target_sd)+30;
				spellaether @target_sd, name2spellid("ÀúÁÖ"), 100;
				message @target_sd, 3, get_mobname(@id) + "ÀÌ(°¡) ÀúÁÖ¸¦ °¡ÇÕ´Ï´Ù";
			}
		}
	}
	if(rand(1, 3) == 2)
	{
			set @target_id, get_mobtarget(@id);
			set @target_sd, id2sd(@target_id);
			if(@target_sd > 0)
			{
				if(!spellaethered(@target_sd, name2spellid("Àı¸Á")))
				{
					if(get_equipitem_name(@target_sd, 6) == "½Ã·ÂÈ¸º¹ÀÇ±Í°ÉÀÌ" || get_equipitem_name(@target_sd, 7) == "½Ã·ÂÈ¸º¹ÀÇ±Í°ÉÀÌ" || spellaethered(@target_sd, name2spellid("Á¤È­ÀÇ´«")) == 1) { end; }

					set_siya @target_sd, 1, 0;
					effect @target_id, 14;
					sound @target_id, 71;
					spellaether @target_sd, name2spellid("Àı¸Á"), 30;
				}
			}
		}
	}
}

@Èä¼±°Ë°´°ø°İ	{
	set @id, objid();
	set @rand, rand(1, 2);
	if(@rand == 1)
	{
		callfunc "MONSTER_RANGE_ATTACK", @id, 10, 8, -1, 44, 188, rand(600000, 700000), 2, "ºÒ", 15, "-1";
	}
}

@Èä¼±¹«»ç°ø°İ	{
	set @id, objid();
	set @side, get_side(@id);
	set @rand, rand(1, 10);
	if(@rand <= 3)
	{
		callfunc "MONSTER_FRONT_ATTACK", @id, 10, 8, -1, 18, 9, rand(1200000, 1500000), 0, "µ¿±Í¾îÁø", "µ¿±Í¾îÁø~!!";
	}
	if(@rand == 1)
	{
	for(set @i, 1; @i < 5; set @i, @i+1)
	{
		switch(@side)
		{
		case 0:
			set @x, get_x(@id);
			set @y, get_y(@id)-@i;
			break;
		case 1:
			set @x, get_x(@id)+@i;
			set @y, get_y(@id);
			break;
		case 2:
			set @x, get_x(@id);
			set @y, get_y(@id)+@i;
			break;
		case 3:
			set @x, get_x(@id)-@i;
			set @y, get_y(@id);
			break;
		}

		set @target_id, obj_getxy(@x, @y);
		if((@x >= mapwidth() || @x < 0) || (@y >= mapheight() || @y < 0 )) { break; }
		else if(@i != 4 && canmove(@id, @x, @y)) { break; };
		
		if(@target_id > 0)
		{
			if(@i != 4) { break; }
			else
			{
				if(istype(@target_id) != 3)
				{
					switch(@side)
					{
					case 0:
						set_y @target_id, get_y(@id)-1;
						set_side @target_id, 2; set @side, 2;
						break;
					case 1:
						set_x @target_id, get_x(@id)+1;
						set_side @target_id, 3; set @side, 3;
						break;
					case 2:
						set_y @target_id, get_y(@id)+1;
						set_side @target_id, 0; set @side, 0;
						break;
					case 3:
						set_x @target_id, get_x(@id)-1;
						set_side @target_id, 1; set @side, 1;
						break;
					}
					if(istype(@target_id) == 1)
					{
						set @target_sd, id2sd(@target_id);
//						updatexy @target_sd;
//						updatelookpc @target_sd, 0;
						updateallpc @target_sd;
						say @id, 2, get_mobname(@id) + ": ÃÊÈ¥ºñ¹«~~!~!";
					}
					else
					{
						move @target_id, @side;
					}
					callfunc "cho_MONSTER_RANGE_ATTACK", objid(), 10, 2, -1, 99, 105, rand(250000, 300000), 0, "ÃÊÈ¥ºñ¹«~~!~!", 16, "-1";
				}
			}
		}
	}
}
	else if(@rand == 2)
	{
	for(set @i, 1; @i < 5; set @i, @i+1)
	{
		switch(@side)
		{
		case 0:
			set @x, get_x(@id);
			set @y, get_y(@id)-@i;
			break;
		case 1:
			set @x, get_x(@id)+@i;
			set @y, get_y(@id);
			break;
		case 2:
			set @x, get_x(@id);
			set @y, get_y(@id)+@i;
			break;
		case 3:
			set @x, get_x(@id)-@i;
			set @y, get_y(@id);
			break;
		}
		
		set @target_id, obj_getxy(@x, @y);
		if((@x >= mapwidth() || @x < 0) || (@y >= mapheight() || @y < 0 )) { break; }
		else if(@i != 4 && canmove(@id, @x, @y)) { break; };
		
		if(@target_id > 0)
		{
			if(@i != 4) { break; }
			else
			{
				switch(@side)
				{
				case 0:
					set_y @id, get_y(@target_id)+1;
					break;
				case 1:
					set_x @id, get_x(@target_id)-1;
					break;
				case 2:
					set_y @id, get_y(@target_id)-1;
					break;
				case 3:
					set_x @id, get_x(@target_id)+1;
					break;
				}
				updatelook @id;
				say @id, 2, get_mobname(@id) + ": ¼âÈ¥ºñ¹«~~!~!";
				callfunc "cho_MONSTER_RANGE_ATTACK", objid(), 10, 2, -1, 100, 97, rand(250000, 300000), 0, "¼âÈ¥ºñ¹«~~!~!", 16, "-1";
				}
			}
		}
	}
}

//@Èä¼±¼±¿ì°ø°İ	{
	set @id, objid();
	set @rand, rand(1, 10);
	if(@rand <= 3)
	{
		callfunc "MONSTER_FRONT_ATTACK", @id, 10, 8, -1, 18, 9, rand(250000, 300000), 0, "µ¿±Í¾îÁø", "µ¿±Í¾îÁø~!!";
	}
}

@Èä¼±¼±¿ì°ø°İ	{
	set @id, objid();
	set @side, get_side(@id);
	set @rand, rand(1, 2);
	if(@rand == 1)
	{
		callfunc "MONSTER_RANGE_ATTACK", @id, 10, 8, -1, 44, 188, rand(600000, 700000), 2, "ºÒ", 15, "-1";
	}
	if(@rand == 1)
	{
	for(set @i, 1; @i < 5; set @i, @i+1)
	{
		switch(@side)
		{
		case 0:
			set @x, get_x(@id);
			set @y, get_y(@id)-@i;
			break;
		case 1:
			set @x, get_x(@id)+@i;
			set @y, get_y(@id);
			break;
		case 2:
			set @x, get_x(@id);
			set @y, get_y(@id)+@i;
			break;
		case 3:
			set @x, get_x(@id)-@i;
			set @y, get_y(@id);
			break;
		}

		set @target_id, obj_getxy(@x, @y);
		if((@x >= mapwidth() || @x < 0) || (@y >= mapheight() || @y < 0 )) { break; }
		else if(@i != 4 && canmove(@id, @x, @y)) { break; };
		
		if(@target_id > 0)
		{
			if(@i != 4) { break; }
			else
			{
				if(istype(@target_id) != 3)
				{
					switch(@side)
					{
					case 0:
						set_y @target_id, get_y(@id)-1;
						set_side @target_id, 2; set @side, 2;
						break;
					case 1:
						set_x @target_id, get_x(@id)+1;
						set_side @target_id, 3; set @side, 3;
						break;
					case 2:
						set_y @target_id, get_y(@id)+1;
						set_side @target_id, 0; set @side, 0;
						break;
					case 3:
						set_x @target_id, get_x(@id)-1;
						set_side @target_id, 1; set @side, 1;
						break;
					}
					if(istype(@target_id) == 1)
					{
						set @target_sd, id2sd(@target_id);
//						updatexy @target_sd;
//						updatelookpc @target_sd, 0;
						updateallpc @target_sd;
						say @id, 2, get_mobname(@id) + ": ÃÊÈ¥ºñ¹«~~!~!";
					}
					else
					{
						move @target_id, @side;
					}
					callfunc "cho_MONSTER_RANGE_ATTACK", objid(), 10, 2, -1, 99, 105, rand(150000, 200000), 0, "ÃÊÈ¥ºñ¹«~~!~!", 16, "-1";
				}
			}
		}
	}
}
	else if(@rand == 2)
	{
	for(set @i, 1; @i < 5; set @i, @i+1)
	{
		switch(@side)
		{
		case 0:
			set @x, get_x(@id);
			set @y, get_y(@id)-@i;
			break;
		case 1:
			set @x, get_x(@id)+@i;
			set @y, get_y(@id);
			break;
		case 2:
			set @x, get_x(@id);
			set @y, get_y(@id)+@i;
			break;
		case 3:
			set @x, get_x(@id)-@i;
			set @y, get_y(@id);
			break;
		}
		
		set @target_id, obj_getxy(@x, @y);
		if((@x >= mapwidth() || @x < 0) || (@y >= mapheight() || @y < 0 )) { break; }
		else if(@i != 4 && canmove(@id, @x, @y)) { break; };
		
		if(@target_id > 0)
		{
			if(@i != 4) { break; }
			else
			{
				switch(@side)
				{
				case 0:
					set_y @id, get_y(@target_id)+1;
					break;
				case 1:
					set_x @id, get_x(@target_id)-1;
					break;
				case 2:
					set_y @id, get_y(@target_id)-1;
					break;
				case 3:
					set_x @id, get_x(@target_id)+1;
					break;
				}
				updatelook @id;
				say @id, 2, get_mobname(@id) + ": ¼âÈ¥ºñ¹«~~!~!";
				callfunc "cho_MONSTER_RANGE_ATTACK", objid(), 10, 2, -1, 100, 97, rand(250000, 300000), 0, "¼âÈ¥ºñ¹«~~!~!", 16, "-1";
				}
			}
		}
	}
}

@Èä¼±Àü»ç°ø°İ	{
	set @id, myid();
	set @side, get_side(@id);
	set @rand, rand(1, 2);
	if(@rand == 1)
	{
	for(set @i, 1; @i < 5; set @i, @i+1)
	{
		switch(@side)
		{
		case 0:
			set @x, get_x(@id);
			set @y, get_y(@id)-@i;
			break;
		case 1:
			set @x, get_x(@id)+@i;
			set @y, get_y(@id);
			break;
		case 2:
			set @x, get_x(@id);
			set @y, get_y(@id)+@i;
			break;
		case 3:
			set @x, get_x(@id)-@i;
			set @y, get_y(@id);
			break;
		}

		set @target_id, obj_getxy(@x, @y);
		if((@x >= mapwidth() || @x < 0) || (@y >= mapheight() || @y < 0 )) { break; }
		else if(@i != 4 && canmove(@id, @x, @y)) { break; };
		
		if(@target_id > 0)
		{
			if(@i != 4) { break; }
			else
			{
				if(istype(@target_id) != 3)
				{
					switch(@side)
					{
					case 0:
						set_y @target_id, get_y(@id)-1;
						set_side @target_id, 2; set @side, 2;
						break;
					case 1:
						set_x @target_id, get_x(@id)+1;
						set_side @target_id, 3; set @side, 3;
						break;
					case 2:
						set_y @target_id, get_y(@id)+1;
						set_side @target_id, 0; set @side, 0;
						break;
					case 3:
						set_x @target_id, get_x(@id)-1;
						set_side @target_id, 1; set @side, 1;
						break;
					}
					if(istype(@target_id) == 1)
					{
						set @target_sd, id2sd(@target_id);
//						updatexy @target_sd;
//						updatelookpc @target_sd, 0;
						updateallpc @target_sd;
						say @id, 2, get_mobname(@id) + ": ÃÊÈ¥ºñ¹«~~!~!";
					}
					else
					{
						move @target_id, @side;
					}
					callfunc "cho_MONSTER_RANGE_ATTACK", objid(), 10, 2, -1, 99, 105, rand(250000, 300000), 0, "ÃÊÈ¥ºñ¹«~~!~!", 16, "-1";
				}
			}
		}
	}
}
	else if(@rand == 2)
	{
	for(set @i, 1; @i < 5; set @i, @i+1)
	{
		switch(@side)
		{
		case 0:
			set @x, get_x(@id);
			set @y, get_y(@id)-@i;
			break;
		case 1:
			set @x, get_x(@id)+@i;
			set @y, get_y(@id);
			break;
		case 2:
			set @x, get_x(@id);
			set @y, get_y(@id)+@i;
			break;
		case 3:
			set @x, get_x(@id)-@i;
			set @y, get_y(@id);
			break;
		}
		
		set @target_id, obj_getxy(@x, @y);
		if((@x >= mapwidth() || @x < 0) || (@y >= mapheight() || @y < 0 )) { break; }
		else if(@i != 4 && canmove(@id, @x, @y)) { break; };
		
		if(@target_id > 0)
		{
			if(@i != 4) { break; }
			else
			{
				switch(@side)
				{
				case 0:
					set_y @id, get_y(@target_id)+1;
					break;
				case 1:
					set_x @id, get_x(@target_id)-1;
					break;
				case 2:
					set_y @id, get_y(@target_id)-1;
					break;
				case 3:
					set_x @id, get_x(@target_id)+1;
					break;
				}
				updatelook @id;
				say @id, 2, get_mobname(@id) + ": ¼âÈ¥ºñ¹«~~!~!";
				callfunc "cho_MONSTER_RANGE_ATTACK", objid(), 10, 2, -1, 100, 97, rand(250000, 300000), 0, "¼âÈ¥ºñ¹«~~!~!", 16, "-1";
				}
			}
		}
	}
}

@Èä¼±´ëÀå±º°ø°İ	{
	set @id, objid();
	set @id, myid();
	set @side, get_side(@id);
	set @rand, rand(1, 2);
	set @rand, rand(1, 10);
	if(@rand <= 2)
	{
		callfunc "MONSTER_FRONT_ATTACK", @id, 10, 8, -1, 18, 9, rand(1000000, 1500000), 0, "µ¿±Í¾îÁø", "µ¿±Í¾îÁø~!!";
	}
	if(@rand == 1)
	{
	for(set @i, 1; @i < 5; set @i, @i+1)
	{
		switch(@side)
		{
		case 0:
			set @x, get_x(@id);
			set @y, get_y(@id)-@i;
			break;
		case 1:
			set @x, get_x(@id)+@i;
			set @y, get_y(@id);
			break;
		case 2:
			set @x, get_x(@id);
			set @y, get_y(@id)+@i;
			break;
		case 3:
			set @x, get_x(@id)-@i;
			set @y, get_y(@id);
			break;
		}

		set @target_id, obj_getxy(@x, @y);
		if((@x >= mapwidth() || @x < 0) || (@y >= mapheight() || @y < 0 )) { break; }
		else if(@i != 4 && canmove(@id, @x, @y)) { break; };
		
		if(@target_id > 0)
		{
			if(@i != 4) { break; }
			else
			{
				if(istype(@target_id) != 3)
				{
					switch(@side)
					{
					case 0:
						set_y @target_id, get_y(@id)-1;
						set_side @target_id, 2; set @side, 2;
						break;
					case 1:
						set_x @target_id, get_x(@id)+1;
						set_side @target_id, 3; set @side, 3;
						break;
					case 2:
						set_y @target_id, get_y(@id)+1;
						set_side @target_id, 0; set @side, 0;
						break;
					case 3:
						set_x @target_id, get_x(@id)-1;
						set_side @target_id, 1; set @side, 1;
						break;
					}
					if(istype(@target_id) == 1)
					{
						set @target_sd, id2sd(@target_id);
//						updatexy @target_sd;
//						updatelookpc @target_sd, 0;
						updateallpc @target_sd;
						say @id, 2, get_mobname(@id) + ": ÃÊÈ¥ºñ¹«~~!~!";
					}
					else
					{
						move @target_id, @side;
					}
					callfunc "cho_MONSTER_RANGE_ATTACK", objid(), 10, 2, -1, 99, 105, rand(250000, 300000), 0, "ÃÊÈ¥ºñ¹«~~!~!", 16, "-1";
				}
			}
		}
	}
}
	else if(@rand == 2)
	{
	for(set @i, 1; @i < 5; set @i, @i+1)
	{
		switch(@side)
		{
		case 0:
			set @x, get_x(@id);
			set @y, get_y(@id)-@i;
			break;
		case 1:
			set @x, get_x(@id)+@i;
			set @y, get_y(@id);
			break;
		case 2:
			set @x, get_x(@id);
			set @y, get_y(@id)+@i;
			break;
		case 3:
			set @x, get_x(@id)-@i;
			set @y, get_y(@id);
			break;
		}
		
		set @target_id, obj_getxy(@x, @y);
		if((@x >= mapwidth() || @x < 0) || (@y >= mapheight() || @y < 0 )) { break; }
		else if(@i != 4 && canmove(@id, @x, @y)) { break; };
		
		if(@target_id > 0)
		{
			if(@i != 4) { break; }
			else
			{
				switch(@side)
				{
				case 0:
					set_y @id, get_y(@target_id)+1;
					break;
				case 1:
					set_x @id, get_x(@target_id)-1;
					break;
				case 2:
					set_y @id, get_y(@target_id)-1;
					break;
				case 3:
					set_x @id, get_x(@target_id)+1;
					break;
				}
				updatelook @id;
				say @id, 2, get_mobname(@id) + ": ¼âÈ¥ºñ¹«~~!~!";
				callfunc "cho_MONSTER_RANGE_ATTACK", objid(), 10, 2, -1, 100, 97, rand(250000, 300000), 0, "¼âÈ¥ºñ¹«~~!~!", 16, "-1";
				}
			}
		}
	}
}
}